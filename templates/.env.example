# CoverIt API reporting is disabled unless this is set to true.
COVERIT_API_REPORTING_ENABLED=false

# Use a URL reachable from the process running the regression tests.
COVERIT_API_BASE_URL=https://api.coverit.example

# Store the real value in GitHub Actions secrets or your local shell, not in coverit.config.json.
COVERIT_API_KEY=
