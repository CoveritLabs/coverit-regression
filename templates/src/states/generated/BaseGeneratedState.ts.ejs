import { BaseState } from "@/core/baseState";
import { StateInfo } from "@/types";

export abstract class BaseGeneratedState extends BaseState {
  protected constructor(definition: StateInfo) {
    super(definition);
  }
}
