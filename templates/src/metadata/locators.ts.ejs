import { LocatorInfo, TransitionLocatorInfo } from "@/types";

export const STATE_LOCATORS = <%- helpers.ts(metadata.locators.states) %> satisfies Record<string, Record<string, LocatorInfo>>;

export const TRANSITION_LOCATORS = <%- helpers.ts(metadata.locators.transitions) %> satisfies Record<string, TransitionLocatorInfo>;
