export interface ApiReportingConfig {
  enabled: boolean;
  baseUrl: string;
  apiKey?: string;
  disabledReason?: string;
}

export interface CoveritConfig {
  application: { baseUrl: string };
  run: { name: string; applicationId: string; versionId?: string };
  apiReporting: ApiReportingConfig;
  reporting: { localArtifactsEnabled: boolean; artifactRoot: string };
  healing: { enabled: boolean; threshold: number };
}
