export interface CoveritConfig {
  application: { baseUrl: string };
  run: { name: string; applicationId: string; versionId?: string };
  api: { enabled: boolean; baseUrl: string; apiKey: string };
  reporting: { localArtifactsEnabled: boolean; artifactRoot: string; apiDisabledReason?: string };
  healing: { enabled: boolean; threshold: number; artifactPath: string };
}
